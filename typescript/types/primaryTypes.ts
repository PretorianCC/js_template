namespace PrimaryTypes {

  // Булево
  const isAdmin = true;
  const isUser = false;

  // Число
  const int: number = 42;
  const float: number = 4.5;
  const num: number = 3e10;

  // Строка
  const str: string = 'template';

  // Массив
  const arr: number[] = [1, 2, 3];
  const arr2: Array<number> = [1, 2, 3];
  
  // Кортеж
  const typle: [string, number] = ['test', 123];

  // Любой тип
  const variable: any = 23;

}